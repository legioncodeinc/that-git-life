# Research Summary: dependency-audit-weapon

Generated by scripture-historian on 2026-05-20.

## Run parameters

- **Depth tier consumed:** normal
- **Time window covered:** 2025-11-01 to 2026-05-20 (approximately 6 months)
- **Files written:** 7 total (1 research-plan.md, 1 research-summary.md, 1 index.md, 1 internal file, 5 external source files)
- **Subfolders:** `internal/` (1 file), `external/` (5 files)

---

## Five most influential sources

### 1. Dependabot vs. Renovate: Operational Experience (safeguard.sh, 2026-02-20)

**Why it matters for weapon-forge:** This is the authoritative 2026 practitioner comparison that weapon-forge should use as the primary evidence base for `guides/00-scanner-decision-matrix.md`. It validates the command brief's directive ("prefer Renovate for teams that need automerge or grouping") with hard data: Renovate supports 90+ package managers vs Dependabot's 30+, works across 5 VCS platforms vs GitHub-only, and its grouping cuts PR volume 3-5x. It also introduces the `minimumReleaseAge` security pattern (7-14 day delay on new releases, informed by the XZ backdoor timeline) that belongs in the lockfile discipline guide.

**Companion source:** systemshardening.com (2026-04-29) provides the complete hardened `renovate.json` with scope rules for npm, Cargo, and Dockerfiles.

### 2. Socket.dev Rust Support GA + Registry Firewall README (socket.dev, 2026-01-19 / 2026-02-16)

**Why it matters for weapon-forge:** Directly answers the command brief's open question about socket.dev's PyPI and Cargo coverage. The answer is YES - socket.dev now covers npm, PyPI, Maven, Cargo, RubyGems, NuGet, Go, and OpenVSX. The registry firewall is an enterprise proxy that blocks malicious packages BEFORE installation. The scanner-decision-matrix guide must update its socket.dev description from "npm-focused threat intelligence tool" to "multi-ecosystem behavioral analysis platform." This also confirms that socket.dev and Snyk are complementary (Snyk catches CVEs; socket catches zero-day behavioral signals).

### 3. How to Generate an SBOM with GitHub Actions in 2026 (safeguard.sh, 2026-03-06)

**Why it matters for weapon-forge:** Contains the canonical 5-step production GitHub Actions SBOM workflow that should be the direct basis for `templates/github-actions-sbom-workflow.yml`. Key decisions it encodes: generate on tag push not branch push; generate from built artifact not source tree; use CycloneDX 1.6 JSON as primary format; use `actions/attest-sbom@v2` for Sigstore-backed attestation; mirror to cold storage for CRA compliance (90-day GitHub artifact retention is insufficient). Also provides the generator priority matrix (cyclonedx-py for Python, cargo-cyclonedx for Rust, Syft as default).

### 4. npm/cli commit 8eff5fb + npm/rfcs #860 (GitHub, 2026-03-18 / 2026-04-02)

**Why it matters for weapon-forge:** Two important data points together. The `--include-attestations` flag (March 2026) gives `npm audit signatures` the ability to output full Sigstore bundles in JSON - important for CI integration. The npm/rfcs #860 issue (triggered by the axios maintainer account hijack in March 2026 - a backdoor published in 40 minutes with no CVE at time of attack) is the clearest evidence that `npm audit` is a CVE compliance tool, not a supply chain security tool. This distinction is critical for `guides/01-vulnerability-triage.md` - the guide must teach engineers when `npm audit` is sufficient and when socket.dev behavioral analysis is required.

### 5. Trail of Bits - Attestations: A new generation of signatures on PyPI + PEP 740 spec

**Why it matters for weapon-forge:** The authoritative source on PyPI attestation state. Key message for `guides/04-provenance-verification.md`: PyPI attestations via PEP 740 are now the default for GitHub Actions Trusted Publishing users (~20,000+ packages, growing), but `pip` and `uv` do NOT yet verify attestations at install time. Trail of Bits is building a pip plugin for verification, and PEP 751 (standardized Python lockfiles) is the path to "trust on first use" semantics. The guide should accurately represent the current state (publisher-side adoption good, consumer-side verification gap) and the roadmap (PEP 751 as the unlock).

---

## Five open questions for the user to resolve

### OQ-1: Snyk free-tier vs paid features for the scanner decision matrix

The research covers Snyk's capabilities but not its 2026 pricing tier details. The command brief references "license scanning add-on" as a Snyk feature - weapon-forge should verify whether license scanning is free-tier or requires a paid plan before encoding it in the decision matrix. This affects the honest cost comparison vs socket.dev.

### OQ-2: OWASP Dependency-Check current state for Java/.NET

The command brief lists OWASP Dependency-Check as a scanner for Java/.NET projects, but the research did not return a 2026 source on its current maintenance state, NVD API v2 migration status, or recommended configuration. The Java ecosystem is in scope per the command brief's expected inputs. Weapon-forge should either conduct a targeted search for "OWASP Dependency-Check 2026 NVD API" or note this as requiring supplemental research.

### OQ-3: Renovate's Mend (hosted) vs self-hosted pricing and feature differences

The research confirms Renovate is AGPL-3.0 (free self-hosted) and that Mend offers a hosted version. The command brief mentions "Renovate (self-hosted or Mend)" but does not specify whether Mend's hosted tier adds features not in the open-source version (e.g., Merge Confidence badges, reachability analysis). The decision matrix guide should distinguish these tiers - but the research doesn't have 2026 Mend pricing details.

### OQ-4: `uv` as the recommended Python package manager vs Poetry vs pip-tools

The research notes `uv lock` as recommended in 2026 for Python lockfile generation. The command brief lists `uv` in the expected inputs alongside `pip/poetry`. Weapon-forge should decide whether to recommend `uv` as the default or remain tool-agnostic in the lockfile discipline guide. This is a directional decision the user should confirm before the guide is authored.

### OQ-5: Cargo/Rust scanner coverage gap for CVE auditing

The research covers `cargo audit` conceptually (via `cargo-deny` patterns in the Renovate config) but does not have a dedicated 2026 source on `cargo audit` vs `cargo-deny` for Rust projects. The command brief's expected inputs include Cargo/go modules. Weapon-forge will need to either leave Rust CVE scanning minimal or conduct a targeted search for "cargo audit cargo-deny 2026" before authoring the scanner decision matrix for Rust projects.

---

## Sources weapon-forge should re-fetch with deeper context

1. **Renovate docs** at `https://docs.renovatebot.com/` - the research used blog summaries; the weapon's Renovate template should reference canonical docs for current config schema version
2. **Snyk CLI docs** at `https://docs.snyk.io/snyk-cli` - not fetched directly; the `templates/snyk-ci-gate.yml` needs current `--severity-threshold` and `--fail-on` flag syntax
3. **Syft release notes** - anchore/sbom-action v0.24.0 (March 2026) should be checked for breaking changes from prior config examples
4. **pnpm v11 audit changelog** - the research captures the GHSA migration but the full v11 breaking change list should be checked for other audit-related changes before encoding in the lockfile guide
