---
ai_description: |
  PRDs actively being implemented. Move the whole PRD folder from ../backlog/
  here when implementation starts. When shipped, move to ../completed/. Never
  partially-state a PRD by editing frontmatter — location is the only source
  of truth for lifecycle.
human_description: |
  PRDs currently in progress.
---

# in-work/

PRDs currently in progress. (Empty until Cursor starts a PRD.)
